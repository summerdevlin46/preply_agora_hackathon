# Hackathon project

3 main branches
- main, only to merge
- dev, work on this
- draft

# How to


Entry point: Gradio UI + Typer CLI
Run:  uv run python app.py serve
Test: uv pytest tests/test_app.py -v

# Requirements

uv
